import 'package:flutter/material.dart';

class UserOrderWidget extends StatelessWidget {
  const UserOrderWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('order has been submited '),
    );
  }
}
